---
import { cx, type WithClasses } from "@/lib/cx";

interface Props extends WithClasses<"root"> {}

const { class: className, classes = {} } = Astro.props;
---

<div
	class={cx([
		"w-full my-3 mx-auto border-b border-border",
		className,
		classes.root
	])}
>
</div>
